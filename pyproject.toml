[tool.poetry]
name = "SenD"
version = "0.1.0"
description = "Python package to infer senescence-leaning cell states and SASP-driven communication."
authors = ["Axel A. Almet <axelalmet@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "^1.23.0"
pandas = "^1.5.0"
scipy = "^1.7.0"
anndata = "^0.8.0"
scanpy = "^1.10.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
